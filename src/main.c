//MARK: Shell loop
//Read, Parse, Execute
void lsh_loop(void) {
    
}

//MARK: Main 
// initialze, interpret, terminate
int main (int argc, char **argv) {
    //load files if any

    //run command loop
    lsh_loop();

    //terminate / perform shutdown/cleanup

    return 0;
}